import React, { FC } from "react";
import { Button } from "@libs/components";

const App: FC = () => {
  return (
    <div>
      <Button label="LAUNCH SLACK" />
    </div>
  );
};

export default App;
