{
  "name": "indirect",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "npm": "8.11.0",
    "node": "16.15.1",
    "pnpm": "7.9.0"
  },
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r build",
    "start": "pnpm -r install && pnpm -F @apps/server serve",
    "heroku-prebuild": "npm i pnpm@7.9.0 && pnpm -r build",
    "prepare": "husky install",
    "dev": "pnpm --parallel dev",
    "pre-commit": "pnpm -r pre-commit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.1"
  }
}
