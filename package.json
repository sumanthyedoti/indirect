{
  "name": "indirect",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "npm": "8.11.0",
    "node": "16.15.1",
    "pnpm": "7.9.0"
  },
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r build",
    "start": "pnpm -F @apps/server serve",
    "prebuild": "npm i pnpm@7.9.0 && pnpm install && pnpm -r install",
    "build:styles": "pnpm -F @libs/styles build:tw",
    "build:server": "pnpm -F @apps/server build",
    "build:web": "pnpm -F @apps/web build",
    "heroku-prebuild": "pnpm prebuild && pnpm build:styles && pnpm build:web && pnpm build:server",
    "prepare": "node -e \"if (process.env.NODE_ENV !== 'production'){process.exit(1)} \" || husky install",
    "dev": "pnpm --parallel dev",
    "pre-commit": "pnpm -r pre-commit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.1"
  }
}
